# Base API URL pointing to backend v1 endpoint
# Backend runs on PORT=5000; this dev server serves on port 8080, so the
# backend's FRONTEND_URL must match or CORS blocks OCR/apply requests.
VITE_API_URL=http://localhost:5000/api/v1
VITE_SENTRY_DSN=

